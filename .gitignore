# Jekyll build artifacts (from local `jekyll build` verification runs)
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Claude Code session state (local working notes, not for the public manual)
.claude/
